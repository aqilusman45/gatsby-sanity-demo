import * as React from 'react'

import TabsSlider from './TabsSlider'

export default {
  title: 'Components/TabsSlider',
  component: TabsSlider,
}

const Template = (args) => <TabsSlider {...args} />

export const Default = Template.bind({})
Default.args = {}
